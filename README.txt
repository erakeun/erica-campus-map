# ERICA Campus Map V2.6.0 · CAMPUS PLACES & SERVICES

## V2.6.0 변경 사항

- 카카오 데이터센터 안산, 인테그리스 코리아 테크놀로지 센터(KTC), 정문 아고라를 주요 장소로 추가했습니다.
- 단과대학 행정팀·학생/행정 지원기관·은행/ATM·복사/출력·편의점 정보를 보완했습니다.
- 정문 아고라의 홍보관 안내와 대관 방법을 한 정보창에서 각각 열 수 있습니다.
- 학생·행정 지원, 편의점, 식당 아이콘을 작은 지도 마커에서도 선명한 SVG 아이콘으로 개선했습니다.
- 다국어 검색, 건물별·층별 시설 그룹, 기존 내부 구조도 링크 동작을 유지합니다.

## V2.5.0 변경 사항

- 선택한 시설을 건물별 마커 하나로 묶고, 상세 창에서 층별로 표시합니다.
- 같은 건물의 전체 시설 보기와 선택한 종류만 보기를 전환할 수 있습니다.
- 시설·업무 검색 시 해당 건물 목록의 일치 항목을 강조합니다.
- 학생회관은 4층 학생지원팀·학사운영팀과 3층 한양보건센터를 함께 안내합니다.
- 모바일 상세 창, 키보드 Escape 닫기, 한국어·영어·중국어를 지원합니다.
- 인접 마커는 겹침을 피하고 연결선으로 원래 건물 위치를 표시합니다.
- 시설 데이터는 FACILITIES의 buildingId 및 floorEn을 기준으로 그룹화합니다.
  floorEn은 3F, 1F, B1 형식으로 유지하고 표시용 floorKo/floorZh도 함께 입력합니다.

## 검증

`node tests/facility-groups.test.cjs`

51개 건물·54개 시설의 누락 및 중복, 건물 참조, 신규 카테고리·안내 링크, 건물·층별 그룹과 지상·지하 층 순서를 검증합니다.

## 이전 버전 배포 안내

## 업로드할 파일

저장소 루트가 아래 구조가 되도록 기존 파일을 교체하면 됩니다.

```text
index.html
assets/
  erica-map-ko.jpg
  erica-map-en.jpg
README.md
```

## 이번 버전

- 상단 `한국어 / English` 전환
- 언어에 따라 한국어·영문 항공지도 자동 교체
- 제목, 검색창, 확대 안내, 건물 목록, 상태 문구, 검색 결과까지 전체 UI 전환
- 한국어·영문 건물명 모두 검색 가능
- 202 영문명 `Solseonggwan`, 204 영문명 `Ilsinjae` 반영
- 현재 영문 화면의 링크는 기존 한국어 블로그 링크로 연결
- 기존 확대/축소, Ctrl/Cmd+휠, 검색 강조 기능 유지
- 확대 상태에서 마우스 드래그 이동 추가
- 선택 언어는 브라우저에 저장되며 영문 주소는 `?lang=en`으로 공유 가능

## 영문 내부구조도 링크를 나중에 추가하는 법

`index.html`에서 해당 건물을 찾습니다.

```js
{
  "id": "101",
  "urlKo": "https://blog.naver.com/...",
  "urlEn": ""
}
```

영문 게시물이 생기면 `urlEn`만 채우면 됩니다.

```js
"urlEn": "https://english-floor-plan-url.example"
```

현재는 `urlEn`이 비어 있으면 영문 화면에서도 `urlKo`로 이동합니다.

## 건물명 수정

- 한국어: `nameKo`
- 영문: `nameEn`
- 이전 명칭이나 추가 검색어: `aliasesKo`, `aliasesEn`

## 점검 주소

클릭 영역을 보려면 배포 주소 뒤에 `?debug=1`을 붙입니다.
영문 디버그 화면은 `?lang=en&debug=1`로 열 수 있습니다.
